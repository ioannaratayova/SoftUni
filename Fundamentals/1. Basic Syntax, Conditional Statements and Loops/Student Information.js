function studentInformation(arg1, arg2, arg3) {
    let name= arg1;
    let age = Number(arg2);
    let grade = Number(arg3).toFixed(2);
    console.log(`Name: ${name}, Age: ${age}, Grade: ${grade}`);
}